diff --git a/tools/demo/test_server.py b/tools/demo/test_server.py
index 4bad1c3..6bd4dd3 100644
--- a/tools/demo/test_server.py
+++ b/tools/demo/test_server.py
@@ -800,6 +800,33 @@ async def diagnostic_cleaner(
             cleaner.config.resample_interval = resample_interval
         df_clean, metadata, audit = cleaner.clean(df_parsed)
         
+        # 瑼Ｘ?邪?賊?甈?
+        topology_columns = ["topology_node_id", "control_semantic", "decay_factor"]
+        present_topology_cols = [col for col in topology_columns if col in df_clean.columns]
+        
+        # 撱箸??邪??
+        topology_summary = {
+            "has_topology": len(present_topology_cols) > 0,
+            "present_columns": present_topology_cols,
+            "node_count": 0,
+            "sample_nodes": []
+        }
+        
+        if "topology_node_id" in df_clean.columns:
+            # 閮??臭?蝭暺嚗???null嚗?+            unique_nodes = df_clean["topology_node_id"].drop_nulls().unique().to_list()
+            topology_summary["node_count"] = len(unique_nodes)
+            topology_summary["sample_nodes"] = unique_nodes[:5]  # ??5 ??暺?+            
+        if "control_semantic" in df_clean.columns:
+            # 蝯梯? control_semantic 憿?
+            semantic_counts = {}
+            for val in df_clean["control_semantic"].drop_nulls().unique().to_list():
+                if val:
+                    count = df_clean.filter(pl.col("control_semantic") == val).shape[0]
+                    semantic_counts[str(val)] = count
+            topology_summary["control_semantic_stats"] = semantic_counts
+        
         return {
             "stage": "cleaner",
             "status": "success",
@@ -811,7 +838,8 @@ async def diagnostic_cleaner(
             "metadata_keys": list(metadata.keys())[:10],
             "audit_keys": list(audit.keys()),
             "quality_flags_present": "quality_flags" in df_clean.columns,
-            "sample_flags": df_clean["quality_flags"].head(5).to_list() if "quality_flags" in df_clean.columns else []
+            "sample_flags": df_clean["quality_flags"].head(5).to_list() if "quality_flags" in df_clean.columns else [],
+            "topology_summary": topology_summary  # ??v1.4 ?啣?
         }
     except Exception as e:
         import traceback
@@ -904,6 +932,53 @@ async def diagnostic_batch_processor(
         results["parse_metadata"] = parse_metadata
         results["overall_status"] = "success" if result.status == "success" else "failed"
         
+        # 瑼Ｘ?邪?賊?甈?
+        topology_columns = ["topology_node_id", "control_semantic", "decay_factor"]
+        present_topology_cols = [col for col in topology_columns if col in df_clean.columns]
+        
+        # 撱箸??邪??
+        topology_summary = {
+            "has_topology": len(present_topology_cols) > 0,
+            "present_columns": present_topology_cols,
+            "node_count": 0,
+            "edge_count": 0,
+            "sample_nodes": [],
+            "sample_edges": []
+        }
+        
+        if "topology_node_id" in df_clean.columns:
+            # 閮??臭?蝭暺嚗???null嚗?+            unique_nodes = df_clean["topology_node_id"].drop_nulls().unique().to_list()
+            topology_summary["node_count"] = len(unique_nodes)
+            topology_summary["sample_nodes"] = unique_nodes[:5]  # ??5 ??暺?+            
+        if "control_semantic" in df_clean.columns:
+            # 蝯梯? control_semantic 憿?
+            semantic_counts = {}
+            for val in df_clean["control_semantic"].drop_nulls().unique().to_list():
+                if val:
+                    count = df_clean.filter(pl.col("control_semantic") == val).shape[0]
+                    semantic_counts[str(val)] = count
+            topology_summary["control_semantic_stats"] = semantic_counts
+            topology_summary["control_semantic_fields"] = len(semantic_counts)
+        
+        # 瑼Ｘ Manifest 銝剔? topology_context
+        manifest_path = output_dir / site_id / "output" / "manifest_v1.3.json"
+        if manifest_path.exists():
+            try:
+                with open(manifest_path, 'r', encoding='utf-8') as f:
+                    manifest = json.load(f)
+                topology_context = manifest.get("topology_context", {})
+                if topology_context:
+                    topology_summary["manifest_has_topology"] = True
+                    topology_summary["manifest_nodes"] = len(topology_context.get("nodes", []))
+                    topology_summary["manifest_edges"] = len(topology_context.get("edges", []))
+                    topology_summary["adjacency_matrix_shape"] = topology_context.get("adjacency_matrix_shape", [0, 0])
+            except Exception:
+                pass
+        
+        results["topology_summary"] = topology_summary  # ??v1.4 ?啣?
+        
     except Exception as e:
         import traceback
         results["overall_status"] = "error"
diff --git a/tools/demo/tester.html b/tools/demo/tester.html
index e193044..f3a4dde 100644
--- a/tools/demo/tester.html
+++ b/tools/demo/tester.html
@@ -377,6 +377,21 @@
                     <!-- ??憿舐內?汗蝯??? -->
                 </div>
 
+                <!-- v1.4 ?邪甈??內 -->
+                <div style="margin-bottom: 15px; padding: 12px; background: rgba(167, 139, 250, 0.1); border: 1px solid rgba(167, 139, 250, 0.3); border-radius: 6px; font-size: 0.85rem;">
+                    <div style="color: var(--purple); font-weight: 500; margin-bottom: 8px;">
+                        ?? v1.4 Excel 蝭?隞乩??邪甈?嚗? GNN 雿輻嚗?
+                    </div>
+                    <div style="color: var(--text-dim); margin-left: 8px;">
+                        <div style="margin: 4px 0;">??<code>topology_node_id</code> - 閮剖??邪蝭暺?ID嚗??GNN ??瑽?</div>
+                        <div style="margin: 4px 0;">??<code>control_semantic</code> - ?批隤儔憿?嚗alve/damper/setpoint嚗?/div>
+                        <div style="margin: 4px 0;">??<code>decay_factor</code> - ?望?扯※皜??賂?0.0-1.0嚗?/div>
+                    </div>
+                    <div style="color: var(--text-dim); margin-top: 8px; font-size: 0.8rem; border-top: 1px solid rgba(167, 139, 250, 0.2); padding-top: 8px;">
+                        ? <strong>?內嚗?/strong>Excel ??description 甈??＊蝷?Point 撠???嚗olumn_name 憿舐內璅?????snake_case ?迂
+                    </div>
+                </div>
+
                 <!-- ?Ｙ?璅∪??豢? -->
                 <div style="margin-bottom: 15px; padding: 12px; background: rgba(0,0,0,0.2); border-radius: 6px;">
                     <div style="font-size: 0.85rem; margin-bottom: 8px; color: var(--text-dim);">?豢??Ｙ??孵?嚗?/div>
@@ -423,6 +438,11 @@
             <p style="font-size: 0.9rem; color: var(--text-dim);">撠??憛怠神憟踝????????末??Excel 璅酉瑼??喉?蝟餌絞??霅?頛臭蒂頧???`.yaml` ?曉
                 config/ 銝准?/p>
 
+            <!-- v1.4 ?內 -->
+            <div style="margin-bottom: 15px; padding: 10px 12px; background: rgba(0, 212, 255, 0.05); border-left: 3px solid var(--cyan); border-radius: 4px; font-size: 0.85rem; color: var(--text-dim);">
+                <strong style="color: var(--cyan);">v1.4 ?舀嚗?/strong>YAML 撠??急?璅貉身摰?topology_node_id, control_semantic, decay_factor嚗?靘?GNN 璅∪?閮毀雿輻
+            </div>
+
             <label>獢?迂 (Site ID):</label>
             <input type="text" id="siteId2" value="demo_site">
             <input type="file" id="excelFile" accept=".xlsx">
@@ -435,9 +455,9 @@
         <!-- Step 4 -->
         <div class="step-card">
             <div class="step-badge">STEP 4 (Sprint 2)</div>
-            <h3>?瑁?摰 ETL Pipeline (Parser -> Cleaner -> BatchProcessor)</h3>
+            <h3>?瑁?摰 ETL Pipeline v1.4 (Parser ??Cleaner ??BatchProcessor)</h3>
             <p style="font-size: 0.9rem; color: var(--text-dim);">?銝 CSV嚗?憪脰?蝺函Ⅳ?芸??菜葫????⊥迤????瘣??蝯撓??BatchProcessor
-                Manifest??br><strong>?蔭璇辣嚗?/strong>隢Ⅱ摰?STEP 2 撌脩????Ｙ?閰脫??游?蝔梁? YAML??/p>
+                Manifest v1.4嚗 GNN ?邪鞈?嚗?br><strong>?蔭璇辣嚗?/strong>隢Ⅱ摰?STEP 2 撌脩????Ｙ?閰脫??游?蝔梁? YAML??/p>
 
             <div id="pipelineParserHint"
                 style="margin: 10px 0 12px 0; color: var(--cyan); font-size: 0.9rem; background: rgba(0, 212, 255, 0.08); border: 1px solid rgba(0, 212, 255, 0.25); border-radius: 6px; padding: 8px 10px;">
@@ -542,14 +562,28 @@
                     </div>
                 </div>
 
-                <h4 style="color:var(--purple); margin-top: 20px;">? ?寞活頛詨 Manifest (v1.3)</h4>
-                <div class="result-box-wrapper">
-                    <button class="copy-btn" onclick="copyToClipboard('manifestOutput', this)" style="right: 120px;">銴ˊ
-                        JSON</button>
-                    <button class="copy-btn" onclick="downloadParquet()"
-                        style="border-color: var(--green); color: var(--green); background: rgba(6, 214, 160, 0.1);">漎?
-                        銝? Parquet</button>
-                    <div class="result-box" id="manifestOutput"></div>
+                <!-- Manifest ??璅豢?閬蒂??-->
+                <div class="grid-2" style="margin-top: 20px;">
+                    <div>
+                        <h4 style="color:var(--purple);">? ?寞活頛詨 Manifest (v1.4)</h4>
+                        <div class="result-box-wrapper">
+                            <button class="copy-btn" onclick="copyToClipboard('manifestOutput', this)" style="right: 120px;">銴ˊ
+                                JSON</button>
+                            <button class="copy-btn" onclick="downloadParquet()"
+                                style="border-color: var(--green); color: var(--green); background: rgba(6, 214, 160, 0.1);">漎?
+                                銝? Parquet</button>
+                            <div class="result-box" id="manifestOutput"></div>
+                        </div>
+                    </div>
+                    <div>
+                        <h4 style="color:var(--purple);">?儭?GNN ?邪??</h4>
+                        <div class="result-box-wrapper">
+                            <button class="copy-btn" onclick="copyToClipboard('topologyOutput', this)">銴ˊ JSON</button>
+                            <div class="result-box" id="topologyOutput" style="max-height: 250px;">
+                                [撠?瑁? Pipeline ??邪鞈?]
+                            </div>
+                        </div>
+                    </div>
                 </div>
 
                 <h4 style="margin-top: 20px;">?? ?? vs 皜?鞈??汗</h4>
@@ -1262,6 +1296,9 @@
                     document.getElementById('parsedOutput').textContent = JSON.stringify(pipelineData.parsed_sample, null, 2);
                     document.getElementById('cleanedOutput').textContent = JSON.stringify(pipelineData.cleaned_sample, null, 2);
 
+                    // 皜脫??邪?? (v1.4 ?啣?)
+                    renderTopologySummary(pipelineData.manifest);
+
                     drawChart(pipelineData.stats);
                     resultSection.style.display = 'block';
                 }
@@ -1435,6 +1472,98 @@
             });
         }
 
+        // 皜脫??邪?? (v1.4 ?啣?)
+        function renderTopologySummary(manifest) {
+            const output = document.getElementById('topologyOutput');
+            
+            if (!manifest) {
+                output.textContent = '[??Manifest 鞈?]';
+                return;
+            }
+
+            const topologyContext = manifest.topology_context || {};
+            const nodes = topologyContext.nodes || [];
+            const edges = topologyContext.edges || [];
+            const adjacencyShape = topologyContext.adjacency_matrix_shape || [0, 0];
+            
+            if (nodes.length === 0 && edges.length === 0) {
+                output.innerHTML = `<div style="color: var(--text-dim);">?? ?芣炎皜砍?邪鞈?<br><span style="font-size: 0.8rem;">Manifest 銝剔 topology_context ??nodes/edges ?箇征</span></div>`;
+                return;
+            }
+
+            // 閮?蝭暺??絞閮?+            const nodeTypes = {};
+            nodes.forEach(node => {
+                const type = node.type || 'unknown';
+                nodeTypes[type] = (nodeTypes[type] || 0) + 1;
+            });
+
+            // 閮? control_semantic 甈?
+            const controlSemanticFields = topologyContext.control_semantic_fields || [];
+
+            let html = `<div style="color: var(--green); font-weight: 500; margin-bottom: 10px;">??撌脰??交?璅貉???/div>`;
+            
+            // ?箸蝯梯?
+            html += `<div style="margin-bottom: 12px; padding: 8px; background: rgba(0,0,0,0.2); border-radius: 6px;">`;
+            html += `<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 8px; font-size: 0.85rem;">`;
+            html += `<div>? 蝭暺?? <strong>${nodes.length}</strong></div>`;
+            html += `<div>?? ?楠?賊?: <strong>${edges.length}</strong></div>`;
+            html += `<div>?? ?唳?拚: <strong>${adjacencyShape[0]}?${adjacencyShape[1]}</strong></div>`;
+            html += `<div>??儭?Control Semantic: <strong>${controlSemanticFields.length} 甈?</strong></div>`;
+            html += `</div></div>`;
+
+            // 蝭暺??絞閮?+            if (Object.keys(nodeTypes).length > 0) {
+                html += `<div style="margin-bottom: 10px;"><div style="color: var(--cyan); font-size: 0.85rem; margin-bottom: 4px;">蝭暺???撣?</div>`;
+                html += `<div style="display: flex; flex-wrap: wrap; gap: 6px;">`;
+                for (const [type, count] of Object.entries(nodeTypes)) {
+                    html += `<span style="background: rgba(167, 139, 250, 0.15); color: var(--purple); padding: 2px 8px; border-radius: 4px; font-size: 0.8rem;">${type}: ${count}</span>`;
+                }
+                html += `</div></div>`;
+            }
+
+            // 蝭暺?閬踝???5 ??
+            if (nodes.length > 0) {
+                html += `<div style="margin-bottom: 10px;"><div style="color: var(--cyan); font-size: 0.85rem; margin-bottom: 4px;">蝭暺?閬?(??5 ??:</div>`;
+                html += `<div style="font-size: 0.8rem;">`;
+                nodes.slice(0, 5).forEach(node => {
+                    const features = (node.features || []).slice(0, 3).join(', ');
+                    const featuresText = features ? ` [${features}${(node.features || []).length > 3 ? '...' : ''}]` : '';
+                    html += `<div style="margin: 2px 0; color: var(--text-dim);">??${node.id} (${node.type})${featuresText}</div>`;
+                });
+                if (nodes.length > 5) {
+                    html += `<div style="color: var(--text-dim); font-size: 0.75rem;">... ?? ${nodes.length - 5} ??暺?/div>`;
+                }
+                html += `</div></div>`;
+            }
+
+            // ?楠?汗嚗? 3 璇?
+            if (edges.length > 0) {
+                html += `<div style="margin-bottom: 10px;"><div style="color: var(--cyan); font-size: 0.85rem; margin-bottom: 4px;">?楠?汗 (??3 璇?:</div>`;
+                html += `<div style="font-size: 0.8rem;">`;
+                edges.slice(0, 3).forEach(edge => {
+                    const relation = edge.relation ? ` (${edge.relation})` : '';
+                    html += `<div style="margin: 2px 0; color: var(--text-dim);">??${edge.source} ??${edge.target}${relation}</div>`;
+                });
+                if (edges.length > 3) {
+                    html += `<div style="color: var(--text-dim); font-size: 0.75rem;">... ?? ${edges.length - 3} 璇?蝺?/div>`;
+                }
+                html += `</div></div>`;
+            }
+
+            // Control Semantic 甈?
+            if (controlSemanticFields.length > 0) {
+                html += `<div><div style="color: var(--cyan); font-size: 0.85rem; margin-bottom: 4px;">Control Semantic 甈?:</div>`;
+                html += `<div style="display: flex; flex-wrap: wrap; gap: 6px;">`;
+                controlSemanticFields.forEach(field => {
+                    html += `<span style="background: rgba(0, 212, 255, 0.1); color: var(--cyan); padding: 2px 8px; border-radius: 4px; font-size: 0.8rem;">${field}</span>`;
+                });
+                html += `</div></div>`;
+            }
+
+            output.innerHTML = html;
+        }
+
         // ====================================================================
         // ?挾?扯那?瑕???(?游???Step 4)
         // ====================================================================
@@ -1505,6 +1634,34 @@
                     html += '</div>';
                 }
 
+                // 憿舐內?邪?? (v1.4 ?啣?)
+                if (data.topology_summary) {
+                    const ts = data.topology_summary;
+                    html += `<div style="margin: 10px 0; padding: 10px; background: rgba(167, 139, 250, 0.1); border: 1px solid rgba(167, 139, 250, 0.3); border-radius: 6px;">`;
+                    html += `<strong style="color: var(--purple);">?儭??邪瑼Ｘ葫:</strong> `;
+                    if (ts.has_topology) {
+                        html += `<span style="color: var(--green);">??撌脰???/span>`;
+                        html += `<div style="margin-top: 6px; font-size: 0.85rem; color: var(--text-dim);">`;
+                        html += `蝭暺? ${ts.node_count || 0} | ?楠: ${ts.edge_count || 0}`;
+                        if (ts.control_semantic_fields !== undefined) {
+                            html += ` | Control Semantic: ${ts.control_semantic_fields} 甈?`;
+                        }
+                        if (ts.present_columns && ts.present_columns.length > 0) {
+                            html += `<br>甈?: ${ts.present_columns.join(', ')}`;
+                        }
+                        if (ts.sample_nodes && ts.sample_nodes.length > 0) {
+                            html += `<br>蝭暺?閬? ${ts.sample_nodes.slice(0, 3).join(', ')}${ts.sample_nodes.length > 3 ? '...' : ''}`;
+                        }
+                        html += `</div>`;
+                    } else {
+                        html += `<span style="color: var(--yellow);">?? ?芣炎皜砍?邪鞈?</span>`;
+                        html += `<div style="margin-top: 6px; font-size: 0.8rem; color: var(--text-dim);">`;
+                        html += `DataFrame 銝剜?曉 topology_node_id?ontrol_semantic ??decay_factor 甈?`;
+                        html += `</div>`;
+                    }
+                    html += '</div>';
+                }
+
                 // 憿舐內?航炊閰單?
                 if (data.error || (data.stages && data.stages.some(s => s.error))) {
                     html += `<div style="margin: 10px 0; padding: 10px; background: rgba(239,71,111,0.1); border: 1px solid #ef476f; border-radius: 6px;">`;
