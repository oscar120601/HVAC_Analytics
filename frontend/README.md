# HVAC Analytics Frontend v3.0

使用 **React 18 + TypeScript + Tailwind CSS + shadcn/ui** 打造的現代化前端介面。

## 🎨 技術棧

- **React 18** - UI 框架
- **TypeScript** - 類型安全
- **Tailwind CSS** - 樣式系統
- **shadcn/ui** - 組件庫（基於 Radix UI）
- **Vite** - 構建工具
- **React Router** - 路由管理
- **Lucide React** - 圖標庫
- **Recharts** - 圖表庫

## 📁 專案結構

```
frontend/
├── src/
│   ├── components/
│   │   ├── ui/           # shadcn/ui 組件
│   │   ├── Layout.tsx    # 布局組件
│   │   └── Sidebar.tsx   # 側邊欄組件
│   ├── pages/
│   │   └── Dashboard.tsx # 主儀表板（包含所有頁面）
│   ├── lib/
│   │   └── utils.ts      # 工具函數
│   ├── types/
│   │   └── index.ts      # 類型定義
│   ├── App.tsx           # 主應用組件
│   ├── main.tsx          # 入口文件
│   └── index.css         # 全局樣式
├── package.json
├── tailwind.config.js
├── tsconfig.json
└── vite.config.ts
```

## 🚀 開始使用

### 1. 安裝依賴

```bash
cd frontend
npm install
```

### 2. 啟動開發伺服器

```bash
npm run dev
```

應用將在 http://localhost:3000 啟動

### 3. 構建生產版本

```bash
npm run build
```

## 🎯 功能特性

### 批次處理模式
- 📋 **解析資料** - 解析並合併 CSV 檔案
- 🧹 **清洗資料** - 資料清洗與物理驗證
- 📊 **統計資訊** - 欄位統計分析
- 📈 **時間序列** - 時間序列視覺化
- 🔗 **關聯矩陣** - 相關性熱圖
- 🎯 **資料品質** - 品質評分儀表板
- 💾 **匯出** - 資料匯出功能

### 最佳化模擬模式
- 🗺️ **特徵映射** - 特徵欄位對應配置
- 🎯 **即時最佳化** - 能耗最佳化計算
- 📊 **特徵重要性** - 模型特徵分析
- 📈 **歷史追蹤** - 最佳化記錄查詢
- 🔧 **模型訓練** - 模型訓練指引

## 🎨 設計特點

### 現代化 UI
- 漸變色彩設計
- 圓角卡片布局
- 陰影層次感
- 平滑動畫過渡

### 響應式設計
- 側邊欄導航
- 自適應佈局
- 可滾動區域

### 主題色系
- **主色**：Indigo/Purple 漸變
- **批次處理**：藍色系 (#3498db)
- **最佳化模擬**：紅色系 (#e74c3c)

## 📝 開發說明

### 添加新頁面

1. 在 `Dashboard.tsx` 中創建頁面組件
2. 在 `Sidebar.tsx` 的菜單項數組中添加對應項目
3. 在 `renderPage` 函數中添加路由邏輯

### 自定義組件

使用 shadcn/ui 組件庫：

```tsx
import { Button } from '@/components/ui/button'
import { Card, CardHeader, CardContent } from '@/components/ui/card'

<Button variant="default">按鈕文字</Button>
```

### 連接後端 API

修改 `vite.config.ts` 中的代理設定：

```ts
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8000',  // Python 後端地址
      changeOrigin: true,
    },
  },
}
```

## 🔮 未來規劃

- [ ] 接入 Python 後端 API
- [ ] 實現資料可視化圖表
- [ ] 添加深色模式
- [ ] 響應式移動端適配
- [ ] 用戶認證系統

## 📄 授權

與主專案相同授權條款。
