# HVAC Analytics - React 前端遷移指南

## 🎉 遷移完成！

已將原本的 Streamlit UI 完全重構為 **React 18 + TypeScript + Tailwind CSS + shadcn/ui**。

## 📊 對比

| 項目 | 舊版 (Streamlit) | 新版 (React) |
|------|------------------|--------------|
| **技術棧** | Python + Streamlit | React 18 + TypeScript |
| **樣式系統** | Streamlit 預設 | Tailwind CSS |
| **組件庫** | Streamlit 原生 | shadcn/ui (50+ 組件) |
| **類型安全** | 無 | TypeScript 完整支援 |
| **路由** | 無 | React Router |
| **開發體驗** | 簡單但受限 | 專業且靈活 |

## 📁 新專案結構

```
frontend/
├── src/
│   ├── components/
│   │   ├── ui/              # shadcn/ui 組件
│   │   │   ├── button.tsx
│   │   │   ├── card.tsx
│   │   │   ├── tabs.tsx
│   │   │   ├── badge.tsx
│   │   │   └── progress.tsx
│   │   ├── Layout.tsx       # 主布局
│   │   └── Sidebar.tsx      # 側邊欄（現代化設計）
│   ├── pages/
│   │   └── Dashboard.tsx    # 所有頁面組件
│   ├── lib/
│   │   └── utils.ts         # 工具函數
│   ├── types/
│   │   └── index.ts         # TypeScript 類型
│   ├── App.tsx              # 路由配置
│   ├── main.tsx             # 入口
│   └── index.css            # Tailwind 樣式
├── package.json
├── tailwind.config.js       # Tailwind 配置
├── tsconfig.json            # TypeScript 配置
└── vite.config.ts           # Vite 配置
```

## 🚀 如何啟動

### 1. 安裝依賴
```bash
cd frontend
npm install
```

### 2. 開發模式
```bash
npm run dev
```
訪問 http://localhost:3000

### 3. 生產構建
```bash
npm run build
```

## 🎨 新 UI 特點

### 側邊欄設計
- ✅ 紫藍漸變標題卡
- ✅ 卡片式模式選擇（批次處理/最佳化模擬）
- ✅ 顏色主題區分（藍色/紅色）
- ✅ 圓點指示器導航
- ✅ 狀態徽章顯示

### 頁面設計
- ✅ 漸變背景卡片
- ✅ 圓角現代風格
- ✅ 陰影層次感
- ✅ 動畫過渡效果

## 📋 功能頁面列表

### 批次處理（7 頁面）
1. 📋 解析資料 - 檔案解析與合併
2. 🧹 清洗資料 - 資料清洗選項
3. 📊 統計資訊 - 欄位統計
4. 📈 時間序列 - 時序圖表
5. 🔗 關聯矩陣 - 相關性分析
6. 🎯 資料品質 - 品質儀表板
7. 💾 匯出 - 資料下載

### 最佳化模擬（5 頁面）
1. 🗺️ 特徵映射 - 欄位對應配置
2. 🎯 即時最佳化 - 最佳化計算
3. 📊 特徵重要性 - 特徵權重
4. 📈 歷史追蹤 - 歷史記錄
5. 🔧 模型訓練 - 訓練指引

## 🔌 連接後端

目前前端是獨立運行的靜態應用。要連接 Python 後端：

### 方式 1：API 代理（推薦）
在 `vite.config.ts` 中配置代理：
```ts
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8000',
      changeOrigin: true,
    },
  },
}
```

### 方式 2：FastAPI + 靜態文件
將 `npm run build` 生成的 `dist/` 目錄掛載到 FastAPI。

### 方式 3：獨立部署
- 前端：部署到 Vercel/Netlify
- 後端：部署到服務器
- 配置 CORS 跨域

## 🛠️ 自定義開發

### 添加新頁面
1. 在 `Dashboard.tsx` 創建組件
2. 在 `Sidebar.tsx` 添加菜單項
3. 更新路由邏輯

### 修改主題色
編輯 `tailwind.config.js`：
```js
colors: {
  hvac: {
    primary: "#3498db",
    secondary: "#2980b9",
    accent: "#e74c3c",
  },
}
```

## ⚠️ 注意事項

1. **這是前端靜態版本** - 尚未連接後端 API
2. **資料是模擬的** - 需要接入真實資料源
3. **需要 Node.js 環境** - 確保已安裝 Node.js 16+

## 📚 相關文件

- `frontend/README.md` - 詳細使用說明
- `frontend/package.json` - 依賴列表
- `ui/README.md` - 舊 Streamlit UI 說明

## 🎉 完成！

新前端已準備就緒。執行 `npm install && npm run dev` 即可體驗全新的現代化介面！
